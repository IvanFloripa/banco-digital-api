const contaCorrente = require('../controller/ContaCorrenteController');

describe('Teste de operações usuário', () =>{
    test('Select usuário',() =>{
        console.log(contaCorrente.router());
    });
})
